apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: workflow-system
spec:
  selector:
    app: notification-service
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
  type: ClusterIP