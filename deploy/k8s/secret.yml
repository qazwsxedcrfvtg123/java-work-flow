apiVersion: v1
kind: Secret
metadata:
  name: workflow-secret
  namespace: workflow-system
type: Opaque
data:
  # base64 encoded values
  MYSQL_ROOT_PASSWORD: cm9vdA==  # "root" in base64
  MYSQL_USER: dXNlcg==  # "user" in base64
  MYSQL_PASSWORD: cGFzc3dvcmQ=  # "password" in base64
  KAFKA_PASSWORD: cGFzc3dvcmQ=  # "password" in base64